spring:
  application:
    name: HystrixApp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
server:
  port: 8989
  
  